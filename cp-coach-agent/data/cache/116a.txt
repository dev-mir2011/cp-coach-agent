{
    "analysis": {
        "hints": {
            "level1": "Consider how the number of passengers in the tram changes at each stop. What information do you need to keep track of?",
            "level2": "The problem asks for the minimum capacity, which implies finding the maximum number of passengers simultaneously present in the tram. How can you calculate the number of passengers after each stop?",
            "level3": "Start with an empty tram (0 passengers). For each stop, first, passengers exit, then new passengers enter. The total capacity must be at least the highest number of passengers ever observed inside the tram.",
            "level4": "Simulate the tram's journey. Maintain a variable for the `current_passengers` and another for the `maximum_passengers_seen`. Update `current_passengers` at each stop, and then update `maximum_passengers_seen` if `current_passengers` exceeds it.",
            "level5": "Initialize `current_passengers = 0` and `max_capacity = 0`. For each stop `i`, read `a_i` and `b_i`. Update `current_passengers = current_passengers - a_i + b_i`. Then, update `max_capacity = max(max_capacity, current_passengers)`. After processing all stops, `max_capacity` will be the answer."
        },
        "summary": {
            "problem_statement": "The problem asks to find the minimum possible capacity a tram must have. Given `n` stops, for each stop `i`, we are provided with the number of passengers `a_i` that exit and `b_i` that enter. The tram starts empty. Passengers exit first, then new passengers enter. The capacity must be sufficient to hold the maximum number of passengers present in the tram at any point during its journey.",
            "input_format": {
                "description": "The first line contains an integer `n`, representing the number of stops. The subsequent `n` lines each contain two integers, `a_i` and `b_i`, indicating the number of passengers exiting and entering at the i-th stop, respectively.",
                "structure": [
                    {
                        "name": "n",
                        "type": "integer",
                        "description": "Number of stops"
                    },
                    {
                        "name": "stops",
                        "type": "array",
                        "description": "List of stop operations",
                        "elements": {
                            "type": "array",
                            "elements": [
                                {
                                    "name": "a_i",
                                    "type": "integer",
                                    "description": "Passengers exiting at stop i"
                                },
                                {
                                    "name": "b_i",
                                    "type": "integer",
                                    "description": "Passengers entering at stop i"
                                }
                            ]
                        }
                    }
                ]
            },
            "output_format": {
                "description": "Print a single integer representing the minimum possible capacity of the tram.",
                "structure": [
                    {
                        "name": "min_capacity",
                        "type": "integer",
                        "description": "The minimum required capacity of the tram"
                    }
                ]
            },
            "constraints": {
                "time_limit": "2 seconds",
                "memory_limit": "256 megabytes",
                "bounds": [
                    {
                        "variable": "n",
                        "min": 2,
                        "max": 1000
                    },
                    {
                        "variable": "a_i",
                        "min": 0,
                        "max": 1000
                    },
                    {
                        "variable": "b_i",
                        "min": 0,
                        "max": 1000
                    },
                    {
                        "variable": "a_1",
                        "value": 0,
                        "description": "No one exits at the first stop"
                    },
                    {
                        "variable": "b_n",
                        "value": 0,
                        "description": "No one enters at the last stop"
                    },
                    {
                        "variable": "passengers_at_last_stop",
                        "value": 0,
                        "description": "All passengers exit at the last stop, tram becomes empty"
                    }
                ]
            },
            "sample_cases": [],
            "key_requirements": [
                "Simulate the number of passengers in the tram at each stop.",
                "Track the maximum number of passengers observed at any given moment.",
                "Output this maximum value as the minimum capacity."
            ]
        },
        "analysis": {
            "key_observation": "The minimum capacity required for the tram is equal to the maximum number of passengers it ever holds simultaneously throughout its journey. We need to track the current passenger count and update a maximum value at each step.",
            "edge_cases": [
                "Minimum number of stops (n=2): The simulation loop handles this naturally.",
                "Tram always empty: If (b_i - a_i) is always non-positive and current_passengers never exceeds 0, the capacity will be 0 (if `max_capacity` is initialized to 0).",
                "High passenger turnover: Large `a_i` and `b_i` values, within limits, can lead to rapid changes in passenger count, requiring careful tracking.",
                "Constraints `a_1=0` and `b_n=0` are problem-specific but handled by the general simulation logic.",
                "The guarantee that `a_i` does not exceed current passengers simplifies the logic, as we don't need to cap `current_passengers` at 0 before adding `b_i`."
            ],
            "naive_failures": [
                "Miscalculating passenger count: Forgetting to subtract `a_i` or add `b_i` at a step.",
                "Incorrectly determining when to update maximum capacity: The maximum should be taken *after* all operations (exiting and entering) for a given stop are complete."
            ],
            "problem_category": {
                "primary": "Simulation",
                "secondary": [
                    "Ad-hoc",
                    "Greedy"
                ],
                "subcategory": "Basic Tracking / Array Processing"
            },
            "complexity_analysis": {
                "constraints": {
                    "N": "1000",
                    "A_i_B_i": "1000"
                },
                "expected_time": "O(N) - We iterate through N stops once to perform constant time operations.",
                "expected_space": "O(1) - We only need to store a few variables (current passengers, max capacity, n, a, b) regardless of N."
            }
        },
        "solution": {
            "language": "Python",
            "approach": "The approach is to simulate the tram's journey stop by stop. We maintain a `current_passengers` count, initialized to zero. At each stop, we first subtract the passengers exiting and then add the passengers entering. After these operations, we compare the `current_passengers` count with a `max_capacity` variable (also initialized to zero) and update `max_capacity` if the current count is higher. After processing all stops, `max_capacity` will hold the required minimum capacity.",
            "time_complexity": "O(N)",
            "space_complexity": "O(1)",
            "key_insights": [
                "The problem asks for the maximum simultaneous occupancy.",
                "A simple iterative simulation correctly tracks occupancy.",
                "No complex data structures or algorithms are needed, just basic arithmetic and comparison."
            ],
            "code": "def solve():\n    n = int(input())\n    \n    current_passengers = 0\n    max_capacity = 0\n    \n    for _ in range(n):\n        a, b = map(int, input().split())\n        \n        current_passengers -= a  # Passengers exit\n        current_passengers += b  # Passengers enter\n        \n        # Update maximum capacity seen so far\n        max_capacity = max(max_capacity, current_passengers)\n        \n    print(max_capacity)\n\nsolve()",
            "explanation": {
                "algorithm_steps": [
                    "Initialize `current_passengers = 0` to represent the tram starting empty.",
                    "Initialize `max_capacity = 0` to store the highest number of passengers observed.",
                    "Loop `n` times, once for each stop:",
                    "  Read the two integers `a` (exiting) and `b` (entering) for the current stop.",
                    "  Update `current_passengers`: subtract `a` (passengers exit) and then add `b` (passengers enter). This order is crucial as per problem statement (exit then enter).",
                    "  Update `max_capacity`: set `max_capacity = max(max_capacity, current_passengers)`. This captures the peak occupancy after all operations at the current stop.",
                    "After the loop finishes, print the final value of `max_capacity`."
                ],
                "edge_cases_handled": [
                    "The minimum `n=2` is handled correctly as the loop runs twice.",
                    "The tram starts empty, which is covered by `current_passengers = 0`.",
                    "The problem constraints `a_1=0` (no exit at first stop) and `b_n=0` (no entry at last stop) are naturally handled by the general arithmetic `current_passengers -= a; current_passengers += b;`. For instance, at the first stop, `a` will be 0, so no passengers are subtracted.",
                    "The guarantee that `a_i` does not exceed the total number of people in the tram simplifies logic, ensuring `current_passengers` never becomes negative before `b_i` is added.",
                    "The final state of the tram (empty at the last stop) does not directly affect the maximum capacity found earlier in the journey, which is what the algorithm correctly tracks."
                ]
            },
            "test_verification": {
                "input": "4\n0 3\n2 5\n4 2\n4 0",
                "expected_output": "6",
                "reasoning": "1. Start: `current_passengers = 0`, `max_capacity = 0`.\n2. Stop 1 (0 3): `current_passengers = 0 - 0 + 3 = 3`. `max_capacity = max(0, 3) = 3`.\n3. Stop 2 (2 5): `current_passengers = 3 - 2 + 5 = 6`. `max_capacity = max(3, 6) = 6`.\n4. Stop 3 (4 2): `current_passengers = 6 - 4 + 2 = 4`. `max_capacity = max(6, 4) = 6`.\n5. Stop 4 (4 0): `current_passengers = 4 - 4 + 0 = 0`. `max_capacity = max(6, 0) = 6`.\nFinal `max_capacity` is 6."
            }
        }
    },
    "code": "#include <bits/stdc++.h>\n\nint main() {\n    std::ios_base::sync_with_stdio(false);\n    std::cin.tie(NULL);\n\n    int n;\n    std::cin >> n;\n\n    int current_passengers = 0;\n    int max_passengers = 0;\n\n    for (int i = 0; i < n; ++i) {\n        int a, b;\n        std::cin >> a >> b;\n        current_passengers -= a;\n        current_passengers += b;\n        if (current_passengers > max_passengers) {\n            max_passengers = current_passengers;\n        }\n    }\n\n    std::cout << max_passengers << std::endl;\n\n    return 0;\n}"
}